/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['links.papareact.com' , 'media.istockphoto.com' , 'plus.unsplash.com']
    }
};

export default nextConfig;
